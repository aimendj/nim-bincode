# Nim compiler configuration
# Enable style checking for usages and errors

--styleCheck: usages
--styleCheck: error

# Add bincode directory to path for source code
--path: "bincode"

# Add nim-stew directory to path for dependencies
--path: "nim-stew"

# Set default output directory for compiled binaries
--outdir: "bin"
